---
import '../styles/global.css'
import { withBase } from '../utils/base'

interface Props {
  title: string
  description?: string
}

const { title, description = 'Pack documentation for Ruby and Rails developers.' } = Astro.props
---

<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <meta name="description" content={description} />
  <meta name="theme-color" content="#080808" />
  <title>{title}</title>
  <link rel="icon" type="image/png" href={withBase('/logo.png')} />
  <script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js"></script>
</head>
<body>
  <div class="docs-shell" x-data="{ open: false }">
    <aside class="docs-side" :class="{ 'is-open': open }">
      <slot name="sidebar" />
    </aside>
    <main class="docs-main">
      <button class="docs-menu" @click="open = !open" :aria-expanded="open">
        <span>Menu</span>
        <span class="docs-menu-mark">/docs</span>
      </button>
      <slot />
    </main>
    <aside class="docs-toc">
      <slot name="toc" />
    </aside>
  </div>

</body>
</html>

<style>
  .docs-shell {
    display: grid;
    grid-template-columns: 240px minmax(0, 1fr) 220px;
    min-height: 100vh;
  }

  .docs-side {
    position: sticky;
    top: 0;
    height: 100vh;
    overflow-y: auto;
    padding: var(--sp-8) var(--sp-5);
    border-right: 1px solid var(--border);
    background: var(--bg);
  }

  .docs-main {
    min-width: 0;
    background: var(--bg);
  }

  .docs-toc {
    position: sticky;
    top: 0;
    height: 100vh;
    overflow-y: auto;
    padding: var(--sp-8) var(--sp-5);
    border-left: 1px solid var(--border);
    background: var(--bg);
  }

  .docs-menu {
    display: none;
    width: calc(100% - (var(--sp-10)));
    margin: var(--sp-5);
    padding: var(--sp-3) var(--sp-4);
    border: 1px solid var(--border);
    border-radius: var(--radius-sm);
    background: var(--bg-subtle);
    color: var(--fg);
    font-family: var(--font-mono);
    font-size: var(--text-xs);
    justify-content: space-between;
    align-items: center;
  }

  .docs-menu-mark {
    color: var(--accent);
  }

  @media (max-width: 1200px) {
    .docs-shell {
      grid-template-columns: 240px minmax(0, 1fr);
    }

    .docs-toc {
      display: none;
    }
  }

  @media (max-width: 960px) {
    .docs-shell {
      grid-template-columns: 1fr;
    }

    .docs-menu {
      display: flex;
    }

    .docs-side {
      position: fixed;
      inset: 0 auto 0 0;
      width: 240px;
      z-index: 100;
      transform: translateX(-100%);
      transition: transform 180ms var(--ease);
    }

    .docs-side.is-open {
      transform: translateX(0);
    }
  }
</style>
